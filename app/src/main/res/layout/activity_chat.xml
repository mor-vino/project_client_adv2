<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <EditText
        android:layout_width="fill_parent"
        android:layout_height="70dp"
        android:background="@drawable/pink_text_box"
        android:hint="Message here..."
        android:textColorHint="#FF0000"
        android:textSize="15dp"
        android:padding="10dp"
        android:id="@+id/act_chat_edit_text"
        android:layout_alignParentBottom="true"
        android:layout_toRightOf="@+id/act_cht_send_btn"
        android:layout_toEndOf="@+id/act_cht_send_btn" />

    <Button
        android:id="@+id/act_cht_send_btn"
        android:layout_width="100dp"
        android:layout_height="70dp"
        android:text="send"
        android:padding="0dp"
        android:background="#FFCC99"
        android:textSize="20dp"
        android:textColor="#FF0000"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

    <ListView
        android:id="@+id/activityChatLV"
        android:layout_width="wrap_content"
        android:layout_height="fill_parent"
        android:dividerHeight="10dp"
        android:background="#FFFF99"
        android:layout_above="@id/act_chat_edit_text"
        ></ListView>
</RelativeLayout>
